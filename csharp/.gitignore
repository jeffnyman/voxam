# Build output and restore state, per project.
bin/
obj/
# Coverage reports the test project writes.
coverage/
# Where a NativeAOT publish lands.
publish/
publish-desktop/

# Where a hang dump lands.
TestResults/
