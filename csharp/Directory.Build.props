<Project>
  <!--
    Settings every project under csharp/ shares. Warnings are errors
    here for the same reason ruff's rules are not advisory in the
    Python: a gate that can be argued with is not a gate.
  -->
  <PropertyGroup>
    <!-- The port versions with the repository; cz bump moves this line. -->
    <Version>2.8.0</Version>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <InvariantGlobalization>false</InvariantGlobalization>
    <Deterministic>true</Deterministic>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
  </PropertyGroup>
</Project>
